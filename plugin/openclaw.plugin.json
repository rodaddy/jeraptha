{
  "id": "jeraptha",
  "name": "Jeraptha Behavioral Enforcement",
  "description": "ECO hooks, wagering system, and Flash Gold task tracking for OpenClaw agents. 17 hooks: 12 before_tool_call (11 blocking + 1 tracker) + 4 before_prompt_build + 1 message_received. v2.6: prompt-include system (auto-inject *.system.include.md / *.transient.include.md from workspace/includes/).",
  "version": "2.6.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true },
      "debug": { "type": "boolean", "default": false },
      "taskFreshnessTurns": { "type": "number", "default": 10 },
      "commGateThreshold": { "type": "number", "default": 8 },
      "heartbeatIntervalMs": { "type": "number", "default": 600000 },
      "gracePeriodTurns": { "type": "number", "default": 5 },
      "conversationFreshnessTurns": { "type": "number", "default": 15 }
    }
  }
}
